# Modernized example (config-driven): run with
#   sudo python hyper2kvm.py --config 50-daemon/generate-systemd.yaml

output_dir: /var/lib/hyper2kvm/out
watch_dir: /var/lib/hyper2kvm/incoming
workdir: /var/lib/hyper2kvm/work
verbose: 1
log_file: /var/log/hyper2kvm.log
